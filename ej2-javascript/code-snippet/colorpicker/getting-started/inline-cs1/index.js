ej.base.enableRipple = true;

// initializes the ColorPicker component
var colorPicker = new ej.inputs.ColorPicker({
        //To render component as inline(flat).
        inline: true,
        showButtons: false
}, '#inline-picker');

