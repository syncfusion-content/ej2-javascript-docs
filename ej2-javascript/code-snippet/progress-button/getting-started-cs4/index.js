// Initialize ProgressButton component
var progressBtn = new ej.splitbuttons.ProgressButton({content: 'Slide Left', enableProgress: true, animationSettings: { effect:'SlideLeft', duration: 500, easing: 'linear' }, spinSettings: { position: 'Center' }});

// Render initialized ProgressButton.
progressBtn.appendTo('#progressbtn');

