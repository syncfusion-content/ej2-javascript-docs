ej.base.enableRipple(true);

// Initialize the Rating control.
var rating = new ej.inputs.Rating({ fullTemplate: '#fullTemplate', emptyTemplate: '#emptyTemplate', value: 3 });

// Render initialized Rating.
rating.appendTo('#rating');

